#include<stdio.h>
int main()
{
	int a[100];
	int n=0;
	for (int i = 0;; i++)
	{
		scanf("%d", &a[i]);
		n++;
		if (a[i] == 0)break;
	}
	for (int j = n-2; j>=0; j--)
	{
		printf("%d ", a[j]);
	}
	return 0;
}
